{% include 'base.html.twig' %}
{% block body %}
<link rel="stylesheet" href="{{ asset('css/style_etat_show.css') }}" type="text/css"/>

    <h1>Detail du type d'utilisateur : {{typeuser.typeUser}}</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ typeuser.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ typeuser.typeUser }}</td>
            </tr>
        </tbody>
    </table>
    <br>
    <a href="{{ path('typeuser_index') }}"><input id="boutton" type="button" value="Revenir a la liste"></a>
    <br><br>
    <a href="{{ path('typeuser_edit', { 'id': typeuser.id }) }}" > <input id="boutton" type="button" value="Modifier"></a>
{% endblock %}
{% include 'footer.html.twig' %}
