{% include 'base.html.twig' %}

{% block body %}

<link rel="stylesheet" href="{{ asset('css/style_etat.css') }}" type="text/css"/>

    <h1>Création d'état</h1>

    <div id="container">

    {{ form_start(form) }}
    <div class="form-box">
        <div class="form-label">
            {{ form_label(form.causeArret) }} :
        </div>
        <div class="form-widget">
            {{ form_widget(form.causeArret) }}
        </div>
    </div>

    </div>

    <div class="form-box">
        <input id="boutton" type="submit" value="Créer cet état" />
        {{ form_end(form) }}
    </div>

        <br>
        <a href="{{ path('etat_index') }}"><input id="boutton" type="button" value="Revenir a la liste"></a>
{% endblock %}
{% include 'footer.html.twig' %}
