{% include 'base.html.twig' %}
{% block body %}
<link rel="stylesheet" href="{{ asset('css/style_etat_show.css') }}" type="text/css"/>

    <h1>Détails de l'état : {{etat.causeArret}}</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ etat.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ etat.causeArret }}</td>
            </tr>
        </tbody>
    </table>
    <br>
    <a href="{{ path('etat_index') }}"><input id="boutton" type="button" value="Revenir a la liste"></a>
    <br><br>
    <a href="{{ path('etat_edit', { 'id': etat.id }) }}" > <input id="boutton" type="button" value="Modifier"></a>
{% endblock %}
{% include 'footer.html.twig' %}
